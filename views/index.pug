extends layout.pug

block nav
    a(class="active" href="/")="Home"
    a(href="/logout")="Logout"
block content

    div(class="container")
        canvas(id="myChart")

    h1="hello"
    if(isAdmin)
        p="admin"

